import React from "react";
import { createRoot } from "react-dom/client";
import CaptionStyleModal from "./CaptionStyleModal";

const root = createRoot(document.getElementById("index"));
root.render(<CaptionStyleModal />);
