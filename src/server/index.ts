import { onOpen, openSidebar } from "./ui";
import { getDocumentInfo, updateCaptions } from "./docs";

// Public functions must be exported as named exports
export { onOpen, openSidebar, getDocumentInfo, updateCaptions };
